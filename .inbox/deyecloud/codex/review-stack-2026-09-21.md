# Review: 2a2306d..9d9482e — HOLD

Target: `/home/l22/Desktop/deyecloud/.tmx-worktrees/main` at `9d9482e6cbb15cd81df8a3284175bb4d1a78abe7`. Includes forecast learning, battery health, year insights, and docs. No source edits, merge, deployment, or external API calls. Existing untracked `.mcp.json.bak-httptest` preserved.

Independent validation: 86/86 tests (12 files), typecheck exit 0, build exit 0. Additional actual-code reproductions used in-memory TypeScript loading, mocked Deye/clock, and SQLite via node:sqlite, including the real Hono battery-health endpoint. All findings below concern newly introduced analytics consuming existing data contracts.

## P2 findings to fix before fast-forward

1. **Unknown SOC becomes a fabricated capacity loss.** `src/lib/battery.ts:44` / `:48` and `src/worker/index.ts:729` accept every non-null SOC, but existing `getLatest` maps missing station SOC plus missing inverter SOC to **0** (`deye.ts:363`), which pollAndStore persists. Confirmed with actual getLatest: missing SOC + BatteryPower=2000 yields soc=0/p=2000. Feeding 59 five-minute samples at SOC=80/P=2000 followed by that missing-SOC zero produces capKwh=12.2917 and SOH=61% against a 20-kWh rating, despite no credible observed discharge-to-zero. DoD simultaneously reports minSoc=80/lowDays=0 because `battery.ts:58` excludes zero. Preserve unknown/invalid readings as unknown at ingestion and reject unqualified historical intervals; do not simply discard genuine zero SOC, which must count in DoD. Add missing-SOC and valid-zero integration regressions, not only ideal linear discharge tests.

2. **Battery rating combines different inverters.** `src/worker/index.ts:738`–742 averages every device's voltage, while ratedAh comes from the first inverter's device_cache. captureTelemetry records all inverter SNs. Actual endpoint with first inverter 400Ah/50V and a second 400V inverter reports nominalV=225, ratedKwh=90, and SOH=22% for a correctly recovered 20-kWh segment (first inverter's rating is 20kWh). Scope the voltage evidence and capacity data to the same identified battery/inverter, fail closed when identity is unavailable, and test mixed SNs. Existing samples can also mix first-inverter live values with station aggregate fallbacks/backfill; do not claim per-pack SOH when that provenance is ambiguous.

3. **Running-month numerator includes today, denominator excludes it; projection double-counts today's yield.** `src/lib/season.ts:23`–25 / `:69`–70. histFromD1's year SUM includes today's daily row; Deye year totals also return the running month. At Sep 21 after today's production, Aug=1240 and Sep=840 (21 days ×40): monthStats reports 42/day instead of 40, then projectYear adds actual=2080 plus 102 remaining days ×40, giving 6160 instead of 6120. At day 1, max(1,td-1) also invents one completed day. Supply a numerator over the same completed-day interval and keep actual-today separate from forecast-remaining-today; alternatively omit partial-month rate until matching data exists. Add first-of-month and current-day-production tests through the history data path.

4. **A past calendar month is assumed fully observed even with one stored day.** `src/lib/season.ts:23` and `:59`–62. A D1 install on Aug 31 with 40kWh and Sep=800 is treated as one full observed month at 40/31=1.29kWh/day; projectYear opens its full-month gate and predicts only ~971.6kWh total on Sep21 with flat PSH. This is separate from the stated choice not to invent production for entirely missing past months: incomplete data is used to depress the FUTURE estimate. Carry coverage/completeness from history roll-ups, gate the baseline on genuinely complete months, and label partial recorded totals. A day-count-only calendar boundary is insufficient. Test mid-month install and interior missing-day coverage.

5. **Known zero-production months disappear from comparisons.** `src/lib/season.ts:25` makes perDay null for an existing row with gen=0. Consequently bestWorst omits the actual worst month, seasonSummary drops its days, and YoY omits a real production collapse. July=1240 and August=0 returns bestWorst=null rather than July best/August worst. Keep verified zero distinct from missing/unknown; incorporate coverage metadata from finding 4 rather than treating every upstream placeholder zero as verified. Add explicit covered-zero vs absent-month regressions.

6. **Primary-station gating uses parameter omission instead of identity.** `src/components/DeviceView.tsx:275` and `src/components/HistoryView.tsx:512`. App passes selectedId whenever stations.length>1, including when that selected ID is the primary. Thus the battery-health card is hidden and the primary year projection loses its PSH data on a multi-station account. Pass the existing fail-closed `isPrimaryStation` identity from App (as Home/Weather already do), independently of the API station parameter. Test primary selected by explicit ID and a sibling selected by ID.

## Requested design checks and nonblocking caveats

- **Forecast ratio and feedback: passed.** Executed actual evaluatedForecastRows/forecastModel against SQLite. A log with cap_kw=10, PSH=5, actual=30 yields ratio=.6 and cond3 factor=.54 even when today's calibrated cap is 12. Replacing predicted=10 with 10000 leaves learned sky unchanged. Stored capacity preserves historic evidence; learned factors do not recursively divide by prior predictions.
- **Day boundary: passed for the running day.** Actual SQL excludes today/future and keeps only day<today. However, a past daily row left at noon because later polls/totals failed is not certified complete merely because midnight passed. There is no completion marker in daily. Avoid describing all evaluated rows as certified final totals; completion/backfill provenance is needed to enforce that stronger claim.
- **30-minute memModel:** bounded server-side drift is acceptable for advisory estimates, but not proof the log equals the exact browser-visible figure. totals_cache can add another cache interval and useForecastModel only loads on mount/primary change. Also client forecast uses today's weather.sun.psh while the logger computes tomorrow's PSH; logger skips until calibrated cap>0 while client can use nameplate/peak. Document the evaluation as the server day-ahead model or share a versioned forecast snapshot if exact UI parity is required.
- **Battery jitter / between-sample charging:** a +1 SOC tick conservatively splits the stretch and may reduce available estimates; accepting hidden charging cannot be ruled out by five-minute endpoint samples. Present capacity/SOH as a sampling-derived estimate, not a certified physical health measurement, and disclose last contributing estimate/sample coverage. The code's latest 10 estimates can be much older than the last 10 calendar days. Ah × measured mid-SOC voltage is the implemented reference ratio, not independently verified manufacturer nominal capacity.
- **D1 cost:** samples(ts PRIMARY KEY) supports the bounded 60-day scan (~17,280 five-minute rows). Exact voltage SQL EXPLAIN QUERY PLAN says SCAN device_samples, not a 30-day index range: its only PK is (sn,ts), so the current time-only query scans up to the retained 180 days (~17,280 rows/inverter), then JSON work on qualifying rows. Scoping to the correct SN in finding 2 also enables the existing composite index. The six-hour cache amortizes repeat requests for one days value, not distinct window values or concurrent cold misses. Counts are structural estimates/local query-plan evidence, not Cloudflare billed-row measurements.
- **Missing entirely past months:** not inventing historical production is reasonable, but label the number as recorded actual plus future estimate and expose coverage; it is not evidence of a complete annual yield. The blocker is using incomplete months as complete future-rate baselines (finding 4).

Requested next step: address the six P2 findings with boundary/identity/provenance regressions, then send the follow-up commit for review. No fast-forward approval for this stack yet.
