# Follow-up review e885fba — HOLD

Target `e885fba176ac838bb880e038106fa7f3fdffabe2` in room/main. Independently 93/93 tests, typecheck/build exit 0, diff --check clean. Read-only review with in-memory TypeScript, fake Deye/clock, and SQLite. No source edits, merge, deployment, or external API calls.

## Remaining P2 findings

1. **socKnown still marks null/blank inverter SOC as valid zero.** `src/worker/deye.ts:397` now sets socKnown=true for every non-null `inv.soc`, but `getInverterFlow`'s numeric reader (`:430`) converts a present `{key:'SOC',value:null}` or empty/whitespace string with Number(value), yielding 0. Reproduced actual getLatest: absent SOC key → socKnown=false (fixed), while value=null, '', or ' ' → soc=0/socKnown=true. These will still be persisted as real zero; the jump filter cannot recover missingness at the beginning of a window, through long gaps, or when a missing zero is close to the previous SOC. Validate raw SOC before coercion in BOTH station and inverter paths; reject null/blank/non-finite/out-of-range values, preserve genuine numeric 0, and retain a valid station reading when inverter SOC is invalid. Add actual getLatest→poll persistence tests for missing key, null, blank, numeric zero, and valid-station/invalid-inverter fallback.

2. **The voltage denominator is now scoped correctly, but capacity samples still mix device and station aggregates.** `src/worker/index.ts:728`–729 reads every unqualified samples row and compares it with one snapshot SN's rating. Existing getLatest falls back to station batteryPower/SOC whenever the first inverter's device fetch fails; backfilled frames are station aggregates too. Actual getLatest with two equal 20-kWh packs and failed inverter snapshot returns a smooth aggregate 4-kW/95→35% six-hour curve; the new cleanSoc accepts all 73 samples, batteryHealth estimates 40kWh, then reports SOH=200% against the first inverter's corrected 20-kWh rating. Correct voltage selection alone does not establish numerator identity. Carry and gate sample source/SN, or conservatively suppress per-pack SOH when multi-inverter/legacy sample scope cannot be proven. Test aggregate fallback and backfill mixing, not only two-SN voltage rows. This remains part of the original same-entity/provenance finding, not a request for a broader redesign.

3. **Version/migrate existing immutable year caches after adding mandatory coverage.** `src/worker/index.ts:954`–957 still accepts every historical hist_v2 cache indefinitely via !isCurrent. Old payloads have monthly gen but no days. New monthStats treats days absent as coverage=0, so YearInsights returns null forever for an otherwise populated cached historical year (also removes the previous-year comparison). Reproduced real HTTP request `/api/history?range=year&date=2025-09-21&station=2` with a 365-day-old pre-upgrade hist_v2 row: 200/cached=true, days undefined, every year rate null, no upstream refresh. Bump the affected cache schema/key or reject/revalidate old year payloads missing coverage. Preserve correct days=0 as known-unknown; do not fabricate calendar coverage merely to revive old caches. Add persisted-old-cache migration regression.

## Confirmed fixed

- Original abrupt legacy80→0 example no longer produces a capacity segment; gradual genuine discharge to zero counts for capacity and DoD (covered by passing tests).
- Actual Hono battery endpoint with A=400Ah/50V and B=400V now returns A nominalV=50/ratedKwh=20/SOH100 for a qualified 20-kWh curve. Removing snapshot SN gives nominalV=null/SOH=null.
- Actual D1 year SQL→monthStats→projectYear with Aug1240/Sep840 including today40 on Sep21 now gives 40/day and total6120 with flat PSH, rather than 42/day and6160. Projection explicitly starts after today; its UI now describes that convention.
- Coverage-aware helper tests preserve genuine covered-zero months and reject partially covered months as baselines.
- App passes explicit isPrimaryStation to DeviceView and HistoryView; gating no longer depends on station query parameter omission.

Nonblocking: BatteryHealth.tsx tooltip is still the original wording (measured/real usable capacity, latest10 days), despite the message saying it was labeled an estimate. It also does not yet display the newly returned lastEstimateDay/coverage. Update those labels as claimed. Fixed SOC change bound is a heuristic, not a universal physical law; describe it accordingly.

No fast-forward approval yet. Send the next follow-up with the three integration regressions above.
